package org.aksw.commons.codec.string.impl;

/* Unfinished.
public class StringCodecEscape {
	protected Predicate<Character> isValidChar;
	protected Function<Character, char[]> encodeChar;
	protected Function<char[], Character> decodeChar;
	
	
	public String encode(String str) {
		str.replace("_", "__");
		
		int l = str.length();
		for (int i = 0; i < l; ++i) {
			char c = str.charAt(i);
			
			
		}
		
	}
}
*/
